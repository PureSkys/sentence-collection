export type sentence = {
    uuid: string,
    sentence: string,
    category: string,
    from_source?: string,
    from_who?: string,
    length: number,
    likes: string,
    createdAt: string,
    updatedAt: string,
}